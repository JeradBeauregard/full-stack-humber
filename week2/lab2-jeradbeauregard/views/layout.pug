block variables
doctype html
html
    head
        meta(charset="utf-8")
        title #{title} | Test website
        link(rel="stylesheet", href="/css/styles.css")
    body
        header#header
            div.header-content
        div#hero
            div#logo
                h1 Handy Candy
                p We love sweet treats
            div#search
                form#productSearch
                    label(for="search") Search for products:
                    input(name="search")
                    button(type="submit").searchButton Search
        div#heroBorder
            nav
                ul
                    each link in menu
                        li: a(href=link.path) #{link.name}
        main#main
            block layout-content
        footer#footer
            p &copy; Copyright HTTP5211, 2022.